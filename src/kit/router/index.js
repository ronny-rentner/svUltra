export { default as Router } from './Router.svelte';
export {
  meta,
  getBaseUrl,
  preloaded,
  currentComponent,
  isLoading,
  currentPath,
  base,
  baseUrl,
  getCurrentPath,
  navigate,
} from './Router.svelte';
